{
  "name": "buildstamp-bin",
  "version": "1.2.1",
  "publishConfig": {
    "access": "public"
  },
  "bin": "./src/main/js/index.js",
  "description": "Buildstamp binaries for MacOS, Linux and Windows",
  "scripts": {
    "clean": "rimraf target",
    "build:bins": "caxa --input ./ --output 'target/bin/buildstamp' -- '{{caxa}}/node_modules/.bin/node' '{{caxa}}/src/main/js/index.js'",
    "build": "yarn build:bins",
    "test": "exit 0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/qiwi/buildstamp.git"
  },
  "keywords": [
    "build-info",
    "buildstamp"
  ],
  "files": [
    "README.md",
    "CHANGELOG.md",
    "target"
  ],
  "license": "MIT",
  "devDependencies": {
    "caxa": "^3.0.1",
    "rimraf": "^5.0.1"
  },
  "dependencies": {
    "buildstamp": "^2.1.4"
  }
}
